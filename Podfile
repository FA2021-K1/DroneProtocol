# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'DroneProtocol' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DroneProtocol
   pod 'CoatySwift', '~> 2.0'

  target 'DroneProtocolTests' do
    # Pods for testing
  end

end


target 'DroneProtocolPackageDescription' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DroneProtocolPackageDescription
  pod 'CoatySwift', '~> 2.0'

end
